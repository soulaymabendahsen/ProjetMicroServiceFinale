# bookyFront/nginx.conf
server {
  listen 80;
  server_name localhost;

  root /usr/share/nginx/html;
  index index.html;

  # Serve Angular app (removed CORS headers - let Gateway handle CORS)
  location / {
    try_files $uri $uri/ /index.html;
  }
}
